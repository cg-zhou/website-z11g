/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: false,
  output: 'standalone',
  allowedDevOrigins: ['localhost', 'local-origin.dev', '*.local-origin.dev', '10.30.12.93'],
}
